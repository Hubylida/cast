依赖安装
npm install
项目启动
node bib/www
项目部署端口号：
127.0.0.1:4000

结构示意图

                      部门一(branch)
            ->部门  -> 部门二(branch)
                      部门三(branch)
                      部门四(branch)

首页(index)  ->活动(activities) ->  单个活动页面(activity)
            
            
            
            ->简介(intro)